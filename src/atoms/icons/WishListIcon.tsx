import * as React from "react";
import WishList from '../assets/wishlist.svg';

const WishListIcon = () => (
    <img src={WishList} style={{width: '20px', height:'20px'}} alt='wishlist'/>
);

export default WishListIcon;