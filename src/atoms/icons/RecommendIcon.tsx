import * as React from "react";
import recommend from '../assets/recommendation.svg'

const RecommendIcon = () => (
    <img src={recommend} style={{width: '20px', height:'20px'}}/>
);

export default RecommendIcon;