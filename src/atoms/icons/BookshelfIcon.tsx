import * as React from "react";
import Bookshelf from '../assets/bookshelf.svg';

const BookshelfIcon = () => (
    <img src={Bookshelf} style={{width: '20px', height:'20px'}} alt='history'/>
);

export default BookshelfIcon;