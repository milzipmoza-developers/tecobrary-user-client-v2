import * as React from "react";
import History from '../assets/history.svg';

const HistoryIcon = () => (
    <img src={History} style={{width: '20px', height:'20px'}} alt='history'/>
);

export default HistoryIcon;