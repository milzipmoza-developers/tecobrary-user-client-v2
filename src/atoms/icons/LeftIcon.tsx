import * as React from "react";
import Left from '../assets/round-left-button.svg';

const LeftIcon = () => (
    <img src={Left} style={{width: '24px', height:'24px'}} alt='left'/>
);

export default LeftIcon;