import * as React from "react";
import {BORDER_DEFAULT_GRAY} from "../common/colors";

const VDivider = () => <div style={{height: '100%', backgroundColor: BORDER_DEFAULT_GRAY, width: '1px'}}/>;

export default VDivider;