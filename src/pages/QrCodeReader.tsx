import * as React from "react";
import DefaultTemplate from "../templates/DefaultTemplate";

const QrCodeReader = () => {
    return (
        <DefaultTemplate title='대여하기' loggedIn={false}>
            대여하기
        </DefaultTemplate>
    )
};

export default QrCodeReader;