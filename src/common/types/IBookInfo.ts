export interface IBookInfo {
    id: number;
    image: string;
    title: string;
    // tslint:disable-next-line:object-literal-sort-keys
    author: string;
    publisher: string;
    isbn: string;
    description: string;
}